import 'package:flutter/material.dart';

class NgulikDart extends StatelessWidget {
  const NgulikDart({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Color(0xff0AC4E0)),
      body: Column(
        children: [
          
        ],
      ),     

    );
  }
}
